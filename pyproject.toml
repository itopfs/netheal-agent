[project]
name = "netheal-agent"
version = "0.1.0"
description = "NetHeal Agent — agentic self-healing network demo"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "app:app"
